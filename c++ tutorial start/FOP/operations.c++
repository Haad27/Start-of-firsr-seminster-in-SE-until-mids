#include <iostream>
using namespace std;

int main (){
//can be int as well but char works as ell as char
    char x=67;
    char y = 90;
    // x+=100;

    // int sum = x+y;
    // cout << sum  ;

    // char numb = 5;
    // char numb2 = 3;
    // returns 1 (true) because 5 is greater than 3
     if (x>y)
    {
    cout << "true";
    }
    else
    {
     cout << "false";
    }
    return 0;
};